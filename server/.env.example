# Server Configuration
PORT=5000
NODE_ENV=development

# Firebase Config
FIREBASE_DATABASE_URL=https://your-project-id.firebaseio.com

# Firebase Service Account (for production deployment) 
# Paste the entire JSON content of your Firebase service account key file
# Make sure it's properly escaped for a single line
# Example: FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"your-project-id",...}
FIREBASE_SERVICE_ACCOUNT=

# Client URL for CORS (add your production frontend URL here)
CLIENT_URL=https://your-frontend-url.com

# Add other environment variables as needed 